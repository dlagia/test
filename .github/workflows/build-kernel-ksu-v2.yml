# ============================================================
#
# Lanjutan dari Fase 1 (build-kernel-ksu-v2.yml) yang SUDAH TERBUKTI
# BOOT NORMAL. File ini menambahkan NoMount (github.com/maxsteeel/nomount)
# di atas basis yang sama persis.
#
# NoMount: framework redirect path level VFS kernel (bukan mount --bind),
# jadi /proc/self/mountinfo tetap stock. Workflow ini mengambil IMPLEMENTASI
# resmi branch `dev` milik maxsteeel/nomount
# (https://github.com/maxsteeel/nomount/tree/dev).
#
# Branch `dev` (default resmi setup.sh) menyediakan integrasi kernel 4.14
# minimal: fs/Kconfig + fs/Makefile, lalu source fs/nomount.c + fs/nomount.h
# ditanam sebagai nomount.o. Tidak lagi memakai patch manual ke fs/namei.c,
# fs/dcache.c, fs/readdir.c, fs/stat.c, fs/proc/task_mmu.c, atau fs/statfs.c.
# Userspace CLI `nm` juga diambil dari branch yang sama agar API kernel/CLI
# tetap sinkron.
#
# VERIFIKASI: kernel/README.md resmi men-default-kan setup.sh ke branch
# `dev`. Patch nomount_4.14_kernel_integration.patch sudah diuji
# `git apply --check` ke source dlagia/android_kernel_xiaomi_rosemary@
# rosemary-13: apply bersih. Teknik hook menukar pointer inode->i_op /
# file->f_op saat runtime (bukan patch source VFS inti).
#
# TEMUAN PENTING (build sebelumnya): modul flashable NoMount yang di-
# download terpisah dari workflow ini TIDAK COCOK dengan kernel yang
# dibangun di sini (risiko beda commit / drift protokol kernel<->userspace).
# FIX: workflow ini MERAKIT SENDIRI module.zip flashable-nya dari clone
# NoMount YANG SAMA PERSIS (commit sama, tidak clone ulang) dengan yang
# dipakai untuk compile kernel - lihat step "Compile nm" dan "Rakit modul
# flashable NoMount" di bawah. Modul hasil build inilah yang harus di-
# flash, BUKAN modul dari sumber lain.
#
# CATATAN: tetap test lewat OrangeFox (backup Boot dulu) sebelum dianggap
# final.
#
# WAJIB: test lewat OrangeFox (backup Boot dulu!) sebelum dianggap final,
# SAMA seperti Fase 1. Kernel ini basis sama dengan yang sudah terbukti
# boot, tapi NoMount menyentuh VFS inti - tetap test dulu.
#
# ReSukiSU diintegrasikan lewat setup.sh RESMI (https://github.com/ReSukiSU/ReSukiSU,
# tanpa argumen branch = checkout "main", sesuai README repo - branch "auto-hook"
# yang dipakai sebelumnya SUDAH DIHAPUS dari workflow ini). Untuk kernel 4.14
# (non-GKI, < 5.10) dipakai CONFIG_KSU_MANUAL_HOOK=y, karena Tracepoint Syscall
# Redirect hanya didukung GKI2 5.10+.
#
# Source resmi branch main TIDAK punya auto_hook_detect.mk/auto_hook.c (itu
# spesifik branch auto-hook yang sudah tidak dipakai). kernel/Kbuild resmi
# meng-include kernel/tools/manual_hook_check.mk yang HARD-FAIL saat compile
# kalau simbol hook tidak ditemukan persis di source - tidak ada fallback
# otomatis. Tiga hook (setuid/init-rc/input) tetap otomatis lewat LSM karena
# CONFIG_KSU_MANUAL_HOOK_AUTO_SETUID_HOOK/AUTO_INITRC_HOOK/AUTO_INPUT_HOOK
# default=y di Kconfig resmi (kernel 4.14 < 6.8, jadi valid dipakai). Sisanya
# (execveat, faccessat, stat, newfstat_ret, fstat64_ret, sys_reboot) DIPATCH
# MANUAL ke fs/exec.c, fs/open.c, fs/stat.c, kernel/reboot.c - setiap anchor
# sudah diverifikasi baris-per-baris terhadap source dlagia/android_kernel_
# xiaomi_rosemary@rosemary-13 sebelum ditanam di sini (lihat step di bawah).
# Patch lain (NoMount/Re:Kernel/Baseband) tetap dipisahkan dari ReSukiSU.
#
# Re:Kernel (github.com/Sakion-Team/Re-Kernel): membuka netlink server
# supaya tombstone/freezer di userspace tahu kapan binder call atau
# signal (SIGKILL/SIGTERM/SIGABRT/SIGQUIT) menyasar proses yang sedang
# di-freeze cgroup, jadi bisa di-unfreeze tepat waktu (kurangi ANR).
# Ditanam manual sesuai Integrate/README.md repo tsb (mode <=5.4).
#
# VERIFIKASI (update): keempat anchor (binder_transaction reply-path +
# transaction-path, binder_alloc_new_buf_locked, do_send_sig_info) SUDAH
# dicocokkan baris-per-baris ke source dlagia/android_kernel_xiaomi_
# rosemary@rosemary-13 dan step Perl-nya SUDAH diuji end-to-end di source
# itu (bukan lagi "tidak ditemukan publik" seperti catatan versi
# sebelumnya - repo itu memang public dan sudah di-clone untuk verifikasi
# ReSukiSU juga). Hasil: ke-4 anchor cocok tepat 1x, brace seimbang,
# rekernel_start_server() muncul 2x di binder.c (reply+transaction path)
# dan 1x masing-masing di binder_alloc.c/signal.c - sesuai ekspektasi.
# CONFIG_KSU_MANUAL_HOOK_AUTO_INITRC_HOOK dari kernel/tools/manual_hook_check.mk
# ReSukiSU dan JOBCTL_TRAP_FREEZE (caveat cgroup-freezer-v2 di README Re:Kernel)
# TIDAK berlaku di kernel 4.14 rosemary-13 ini (belum backport freezer v2).
# Pengecekan "match harus tepat 1x" di apply_rekernel_hooks.pl tetap
# dipertahankan sebagai pengaman kalau upstream kernel source berubah.
#
# CATATAN (permintaan eksplisit): penambalan CVE-2026-43499 (GhostLock,
# rtmutex/futex UAF -> local root exploit) yang sebelumnya ada di workflow
# ini SUDAH DIHAPUS TOTAL atas permintaan langsung - baik step-nya, patch
# base64-nya, maupun seluruh referensi ke CVE ini di komentar. Kernel hasil
# build ini TIDAK lagi menyertakan backport fix untuk bug rt_mutex/futex
# remove_waiter() tsb.
# ============================================================

on:
  workflow_dispatch: {}
  push:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 120

    steps:
      - name: Pasang dependency build
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            bc bison build-essential ccache cpio curl flex git kmod \
            libelf-dev libssl-dev zip zlib1g-dev xxd \
            libtinfo5 libncurses5 gcc-aarch64-linux-gnu

      - name: Clone source kernel (dlagia/android_kernel_xiaomi_rosemary @ rosemary-13)
        run: |
          git clone --depth=1 -b rosemary-13 \
            https://github.com/dlagia/android_kernel_xiaomi_rosemary.git kernel

      - name: Samakan SUBLEVEL dengan versi stock (4.14.186) - hasil temuan fsck.f2fs
        working-directory: kernel
        run: |
          # fsck.f2fs vendor Xiaomi (biner terpisah, di luar build ini) hardcode
          # "4.14.186-perf" sebagai versi kernel yang ia harapkan. Supaya uname -r
          # kernel custom ini SAMA PERSIS dengan itu (bukan cuma LOCALVERSION-nya,
          # tapi angka SUBLEVEL asli di Makefile), ubah di sini SEBELUM apa pun
          # lain dibangun - supaya konsisten di seluruh proses compile & versi
          # LINUX_VERSION_CODE yang dipakai kode sumber (masih di rentang 4.14.x
          # yang sama, jadi tidak mempengaruhi cabang kondisional versi manapun
          # di patch ReSukiSU/NoMount/Re-Kernel).
          grep -n '^SUBLEVEL = ' Makefile
          sed -i 's/^SUBLEVEL = .*/SUBLEVEL = 186/' Makefile
          grep -n '^SUBLEVEL = ' Makefile

      - name: Integrasikan ReSukiSU (setup.sh resmi, branch main)
        working-directory: kernel
        run: |
          # Aturan resmi repo (https://github.com/ReSukiSU/ReSukiSU): setup.sh
          # TANPA argumen -> clone jadi KernelSU/, checkout branch "main".
          # (Argumen ke setup.sh adalah commit/tag opsional, BUKAN nama fitur
          # seperti "auto-hook" - branch itu sudah tidak dipakai di workflow ini.)
          curl -LSs "https://raw.githubusercontent.com/ReSukiSU/ReSukiSU/main/kernel/setup.sh" | bash

          test -d KernelSU || {
            echo "ERROR: direktori KernelSU tidak ditemukan"
            exit 1
          }

          echo "== ReSukiSU branch =="
          cd KernelSU
          git branch --show-current
          git rev-parse --short HEAD
          test "$(git branch --show-current)" = "main" || {
            echo "ERROR: ReSukiSU bukan branch main (setup.sh resmi tanpa argumen harus checkout main)"
            exit 1
          }
          cd ..

          test -L drivers/kernelsu || {
            echo "ERROR: symlink drivers/kernelsu tidak dibuat oleh setup.sh"
            exit 1
          }
          grep -q 'obj-$(CONFIG_KSU) += kernelsu/' drivers/Makefile || {
            echo "ERROR: drivers/Makefile tidak berisi entry kernelsu/"
            exit 1
          }
          grep -q 'source "drivers/kernelsu/Kconfig"' drivers/Kconfig || {
            echo "ERROR: drivers/Kconfig tidak berisi source kernelsu/Kconfig"
            exit 1
          }

          echo "ReSukiSU main (resmi): OK"
          ls -ld KernelSU drivers/kernelsu

      - name: Pasang hook manual ReSukiSU (kernel 4.14, non-GKI)
        working-directory: kernel
        run: |
          # kernel/tools/manual_hook_check.mk (bagian dari branch main resmi) akan
          # HARD-FAIL saat compile kalau simbol berikut tidak ditemukan persis di
          # source - tidak ada fallback otomatis di branch resmi ini. Anchor di
          # bawah sudah dicocokkan baris-per-baris ke source dlagia/android_kernel_
          # xiaomi_rosemary@rosemary-13 (bukan tebakan/template generik) sebelum
          # ditanam. Tiga hook lain (setuid/init-rc/input) SENGAJA tidak dipatch di
          # sini karena CONFIG_KSU_MANUAL_HOOK_AUTO_SETUID_HOOK/AUTO_INITRC_HOOK/
          # AUTO_INPUT_HOOK sudah default=y di Kconfig resmi (valid untuk kernel
          # 4.14, karena opsi itu cuma dilarang di kernel >= 6.8) - keduanya
          # ditangani otomatis lewat LSM oleh ReSukiSU sendiri.
          python3 - << 'PYEOF'
          import sys

          def patch(path, replacements):
              with open(path, "r") as f:
                  s = f.read()
              for old, new, label in replacements:
                  n = s.count(old)
                  if n != 1:
                      print(f"ERROR: anchor '{label}' di {path} ditemukan {n}x (harus persis 1x)")
                      sys.exit(1)
                  s = s.replace(old, new, 1)
              with open(path, "w") as f:
                  f.write(s)
              print(f"{path}: OK")

          # ---------- fs/exec.c: ksu_handle_execveat ----------
          exec_decl_old = (
              "static int do_execveat_common(int fd, struct filename *filename,\n"
              "\t\t\t      struct user_arg_ptr argv,\n"
              "\t\t\t      struct user_arg_ptr envp,\n"
              "\t\t\t      int flags)\n{\n\tchar *pathbuf = NULL;"
          )
          exec_decl_new = (
              "#ifdef CONFIG_KSU\n"
              "extern int ksu_handle_execveat(int *fd, struct filename **filename_ptr, void *argv,\n"
              "\t\t\t\tvoid *envp, int *flags);\n"
              "#endif\n\n"
          ) + exec_decl_old
          exec_body_old = (
              "\tint retval;\n\n\tif (IS_ERR(filename))\n\t\treturn PTR_ERR(filename);"
          )
          exec_body_new = (
              "\tint retval;\n\n"
              "#ifdef CONFIG_KSU\n"
              "\tksu_handle_execveat(&fd, &filename, &argv, &envp, &flags);\n"
              "#endif\n\n"
              "\tif (IS_ERR(filename))\n\t\treturn PTR_ERR(filename);"
          )
          patch("fs/exec.c", [
              (exec_decl_old, exec_decl_new, "do_execveat_common signature"),
              (exec_body_old, exec_body_new, "do_execveat_common body"),
          ])

          # ---------- fs/open.c: ksu_handle_faccessat ----------
          open_old = (
              "SYSCALL_DEFINE3(faccessat, int, dfd, const char __user *, filename, int, mode)\n"
              "{\n\tconst struct cred *old_cred;\n\tstruct cred *override_cred;\n"
              "\tstruct path path;\n\tstruct inode *inode;\n\tstruct vfsmount *mnt;\n"
              "\tint res;\n\tunsigned int lookup_flags = LOOKUP_FOLLOW;\n\n"
              "\tif (mode & ~S_IRWXO)\t/* where's F_OK, X_OK, W_OK, R_OK? */\n"
              "\t\treturn -EINVAL;"
          )
          open_new = (
              "#ifdef CONFIG_KSU\n"
              "extern int ksu_handle_faccessat(int *dfd, const char __user **filename_user, int *mode,\n"
              "\t\t\t\t int *flags);\n#endif\n\n"
              "SYSCALL_DEFINE3(faccessat, int, dfd, const char __user *, filename, int, mode)\n"
              "{\n\tconst struct cred *old_cred;\n\tstruct cred *override_cred;\n"
              "\tstruct path path;\n\tstruct inode *inode;\n\tstruct vfsmount *mnt;\n"
              "\tint res;\n\tunsigned int lookup_flags = LOOKUP_FOLLOW;\n\n"
              "#ifdef CONFIG_KSU\n\tksu_handle_faccessat(&dfd, &filename, &mode, NULL);\n#endif\n\n"
              "\tif (mode & ~S_IRWXO)\t/* where's F_OK, X_OK, W_OK, R_OK? */\n"
              "\t\treturn -EINVAL;"
          )
          patch("fs/open.c", [(open_old, open_new, "SYSCALL_DEFINE3(faccessat...)")])

          # ---------- fs/stat.c: ksu_handle_stat + newfstat_ret + fstat64_ret ----------
          stat_extern = (
              "#ifdef CONFIG_KSU\n"
              "extern int ksu_handle_stat(int *dfd, const char __user **filename_user, int *flags);\n"
              "extern void ksu_handle_newfstat_ret(unsigned int *fd, struct stat __user **statbuf_ptr);\n"
              "#if defined(__ARCH_WANT_STAT64) || defined(__ARCH_WANT_COMPAT_STAT64)\n"
              "extern void ksu_handle_fstat64_ret(unsigned long *fd, struct stat64 __user **statbuf_ptr);\n"
              "#endif\n#endif\n\n"
          )
          newfstatat_old = (
              "SYSCALL_DEFINE4(newfstatat, int, dfd, const char __user *, filename,\n"
              "\t\tstruct stat __user *, statbuf, int, flag)\n{\n\tstruct kstat stat;\n"
              "\tint error;\n\n\terror = vfs_fstatat(dfd, filename, &stat, flag);"
          )
          newfstatat_new = stat_extern + (
              "SYSCALL_DEFINE4(newfstatat, int, dfd, const char __user *, filename,\n"
              "\t\tstruct stat __user *, statbuf, int, flag)\n{\n\tstruct kstat stat;\n"
              "\tint error;\n\n"
              "#ifdef CONFIG_KSU\n\tksu_handle_stat(&dfd, &filename, &flag);\n#endif\n\n"
              "\terror = vfs_fstatat(dfd, filename, &stat, flag);"
          )
          newfstat_old = (
              "SYSCALL_DEFINE2(newfstat, unsigned int, fd, struct stat __user *, statbuf)\n"
              "{\n\tstruct kstat stat;\n\tint error = vfs_fstat(fd, &stat);\n\n"
              "\tif (!error)\n\t\terror = cp_new_stat(&stat, statbuf);\n\n\treturn error;\n}"
          )
          newfstat_new = (
              "SYSCALL_DEFINE2(newfstat, unsigned int, fd, struct stat __user *, statbuf)\n"
              "{\n\tstruct kstat stat;\n\tint error = vfs_fstat(fd, &stat);\n\n"
              "\tif (!error)\n\t\terror = cp_new_stat(&stat, statbuf);\n\n"
              "#ifdef CONFIG_KSU\n\tif (!error)\n\t\tksu_handle_newfstat_ret(&fd, &statbuf);\n#endif\n\n"
              "\treturn error;\n}"
          )
          fstat64_old = (
              "SYSCALL_DEFINE2(fstat64, unsigned long, fd, struct stat64 __user *, statbuf)\n"
              "{\n\tstruct kstat stat;\n\tint error = vfs_fstat(fd, &stat);\n\n"
              "\tif (!error)\n\t\terror = cp_new_stat64(&stat, statbuf);\n\n\treturn error;\n}"
          )
          fstat64_new = (
              "SYSCALL_DEFINE2(fstat64, unsigned long, fd, struct stat64 __user *, statbuf)\n"
              "{\n\tstruct kstat stat;\n\tint error = vfs_fstat(fd, &stat);\n\n"
              "\tif (!error)\n\t\terror = cp_new_stat64(&stat, statbuf);\n\n"
              "#ifdef CONFIG_KSU\n\tif (!error)\n\t\tksu_handle_fstat64_ret(&fd, &statbuf);\n#endif\n\n"
              "\treturn error;\n}"
          )
          patch("fs/stat.c", [
              (newfstatat_old, newfstatat_new, "SYSCALL_DEFINE4(newfstatat...) + extern block"),
              (newfstat_old, newfstat_new, "SYSCALL_DEFINE2(newfstat...)"),
              (fstat64_old, fstat64_new, "SYSCALL_DEFINE2(fstat64...)"),
          ])

          # ---------- kernel/reboot.c: ksu_handle_sys_reboot (ReSukiSU supercall) ----------
          reboot_old = (
              "SYSCALL_DEFINE4(reboot, int, magic1, int, magic2, unsigned int, cmd,\n"
              "\t\tvoid __user *, arg)\n{\n"
              "\tstruct pid_namespace *pid_ns = task_active_pid_ns(current);\n"
              "\tchar buffer[256];\n\tint ret = 0;\n\n"
              "\t/* We only trust the superuser with rebooting the system. */"
          )
          reboot_new = (
              "#ifdef CONFIG_KSU\n"
              "extern int ksu_handle_sys_reboot(int magic1, int magic2, unsigned int cmd, void __user **arg);\n"
              "#endif\n\n"
              "SYSCALL_DEFINE4(reboot, int, magic1, int, magic2, unsigned int, cmd,\n"
              "\t\tvoid __user *, arg)\n{\n"
              "\tstruct pid_namespace *pid_ns = task_active_pid_ns(current);\n"
              "\tchar buffer[256];\n\tint ret = 0;\n\n"
              "#ifdef CONFIG_KSU\n"
              "\t/* ReSukiSU supercall: manager pasang fd komunikasi lewat syscall reboot()\n"
              "\t * dengan magic1/magic2 khusus KSU (BUKAN LINUX_REBOOT_MAGIC*).\n"
              "\t * ksu_handle_sys_reboot() sendiri sudah memvalidasi magic1 dan\n"
              "\t * mengembalikan -EINVAL kalau bukan supercall - reboot asli tak terpengaruh. */\n"
              "\tif (!ksu_handle_sys_reboot(magic1, magic2, cmd, &arg))\n\t\treturn 0;\n"
              "#endif\n\n"
              "\t/* We only trust the superuser with rebooting the system. */"
          )
          patch("kernel/reboot.c", [(reboot_old, reboot_new, "SYSCALL_DEFINE4(reboot...)")])
          PYEOF

          echo ""
          echo "== Verifikasi simbol hook manual tertanam =="
          for pair in \
            "fs/exec.c:ksu_handle_execveat" \
            "fs/open.c:ksu_handle_faccessat" \
            "fs/stat.c:ksu_handle_stat" \
            "fs/stat.c:ksu_handle_newfstat_ret" \
            "fs/stat.c:ksu_handle_fstat64_ret" \
            "kernel/reboot.c:ksu_handle_sys_reboot"; do
            f="${pair%%:*}"; sym="${pair##*:}"
            grep -q "$sym" "$f" || { echo "ERROR: $sym tidak ditemukan di $f"; exit 1; }
            echo "OK: $sym -> $f"
          done

      - name: Integrasikan NoMount (dev)
        working-directory: kernel
        run: |
          # NoMount branch: maxsteeel/nomount dev
          # https://github.com/maxsteeel/nomount/tree/dev
          # Ikuti struktur resmi branch ini untuk kernel 4.14:
          #   fs/nomount -> ../NoMount/kernel/src
          #   fs/Kconfig -> source "fs/nomount/Kconfig"
          #   fs/Makefile -> obj-$(CONFIG_NOMOUNT) += nomount/
          # Tidak menyalin nomount.c/nomount.h langsung ke fs/.
          # Tidak menyuntik hook manual ke fs/namei.c, fs/dcache.c,
          # fs/readdir.c, fs/stat.c, fs/proc/task_mmu.c, atau fs/statfs.c.
          #
          # PENTING: clone NoMount di sini adalah SATU-SATUNYA clone dipakai
          # untuk SELURUH integrasi NoMount di workflow ini (kernel driver,
          # kompilasi nm, DAN modul flashable) - lihat step "Compile nm" dan
          # "Rakit modul flashable NoMount" di bawah, keduanya memakai folder
          # NoMount/ ini juga, TIDAK clone ulang. Ini sengaja untuk menjamin
          # commit driver kernel dan commit userspace/modul SELALU sama persis.

          rm -rf NoMount
          git clone --depth=1 -b dev \
            https://github.com/maxsteeel/nomount.git NoMount

          echo "NoMount commit dipakai (kernel + modul + nm): $(git -C NoMount rev-parse --short HEAD)"

          # Pastikan source branch yang dipakai benar-benar memiliki
          # layout kernel/src yang diharapkan oleh patch 4.14.
          test -s NoMount/kernel/src/Kconfig
          test -s NoMount/kernel/src/Makefile
          test -s NoMount/kernel/src/nomount.c
          test -s NoMount/kernel/src/nomount.h

          # fs/nomount adalah symlink ke kernel/src, sesuai setup.sh resmi.
          rm -rf fs/nomount
          ln -s ../NoMount/kernel/src fs/nomount

          # Terapkan patch integrasi resmi 4.14 dari branch dev.
          git apply --verbose NoMount/kernel/patches/nomount_4.14_kernel_integration.patch

          echo "==================== NoMount (dev) ===================="
          echo "NoMount source: $(readlink -f fs/nomount)"
          echo ""
          echo "== fs/Kconfig =="
          grep -nF 'source "fs/nomount/Kconfig"' fs/Kconfig
          echo "== fs/Makefile =="
          grep -nF 'obj-$(CONFIG_NOMOUNT) += nomount/' fs/Makefile
          echo "== NoMount source =="
          test -L fs/nomount
          test -s fs/nomount/Kconfig
          test -s fs/nomount/Makefile
          test -s fs/nomount/nomount.c
          test -s fs/nomount/nomount.h
          grep -n '^config NOMOUNT' fs/nomount/Kconfig
          grep -n 'obj-$(CONFIG_NOMOUNT).*nomount.o' fs/nomount/Makefile

          # Pastikan hook NoMount lama TIDAK ikut masuk ke source kernel.
          if grep -R -nE 'nomount_hooks.patch|nomount_handle_(getname|permission|dpath|iterate_dir|getattr)|nomount_spoof_(mmap_metadata|statfs)' \
              fs/namei.c fs/dcache.c fs/readdir.c fs/stat.c fs/proc/task_mmu.c fs/statfs.c 2>/dev/null; then
            echo "ERROR: hook NoMount lama masih terdeteksi di source kernel"
            exit 1
          fi

          echo "NoMount dev: integration OK"

      - name: Integrasikan Re:Kernel (Sakion-Team) - notifikasi freeze binder/signal
        working-directory: kernel
        run: |
          # https://github.com/Sakion-Team/Re-Kernel
          # Header/rekernel.c disalin sesuai contoh kode di Integrate/README.md resmi
          # (konstanta NETLINK 22-26, USER_PORT 100, PACKET_SIZE 128, MIN_USERAPP_UID
          # 10000, MAX_SYSTEM_UID 2000, RESERVE_ORDER 17 - dicocokkan persis ke README).
          # Fungsi statis di README (send_netlink_message, start_rekernel_server,
          # line_is_frozen) dipindah ke satu translation unit drivers/rekernel/rekernel.c
          # dengan prefix rekernel_ (supaya tidak multiple-definition, karena README
          # menaruhnya di header yang di-include 3 file berbeda) - fungsinya identik,
          # cuma dipindah lokasi + di-rename. Anchor di bawah SUDAH diverifikasi cocok
          # tepat 1x ke source dlagia/android_kernel_xiaomi_rosemary@rosemary-13 (lihat
          # catatan di header file ini). "match harus tepat 1x" tetap dipertahankan
          # sebagai pengaman kalau upstream kernel source berubah.
          mkdir -p drivers/rekernel
          echo "" | base64 -d > drivers/rekernel/rekernel.h
          echo "" | base64 -d > drivers/rekernel/rekernel.c
          echo "b2JqLSQoQ09ORklHX1JFS0VSTkVMKSArPSByZWtlcm5lbC5vCg==" | base64 -d > drivers/rekernel/Makefile
          echo "" | base64 -d > drivers/rekernel/Kconfig

          grep -q 'rekernel/' drivers/Makefile || echo 'obj-y += rekernel/' >> drivers/Makefile
          grep -q 'rekernel/Kconfig' drivers/Kconfig || echo 'source "drivers/rekernel/Kconfig"' >> drivers/Kconfig

          sed -i '1i #include "../rekernel/rekernel.h"' drivers/android/binder.c
          sed -i '1i #include "../rekernel/rekernel.h"' drivers/android/binder_alloc.c
          sed -i '1i #include "../drivers/rekernel/rekernel.h"' kernel/signal.c

          echo "" | base64 -d > /tmp/apply_rekernel_hooks.pl
          perl /tmp/apply_rekernel_hooks.pl

          grep -c 'rekernel_start_server' drivers/android/binder.c drivers/android/binder_alloc.c kernel/signal.c

      - name: Integrasikan Baseband-guard (LSM anti-tulis baseband/boot chain)
        working-directory: kernel
        run: |
          # https://github.com/vc-teahouse/Baseband-guard
          # LSM ringan yang memblokir tulisan tak sah ke partisi/device node kritis
          # (baseband, boot chain) di level kernel. Dipasang via setup.sh resmi milik
          # proyek ini sendiri (pola sama seperti setup.sh ReSukiSU) - bukan patch
          # tertanam manual, jadi selalu ambil versi terbaru tiap build.
          #
          # VERIFIKASI (sudah diuji end-to-end di source dlagia/android_kernel_
          # xiaomi_rosemary@rosemary-13): kernel 4.14 ini TIDAK punya
          # `#define DEFINE_LSM(lsm)` di include/linux/lsm_hooks.h (gaya LSM lama,
          # pra-5.1). setup.sh MENDETEKSI ini secara otomatis dan menerapkan patch
          # SELinux legacy (sepatch.txt ditempel ke security/selinux/Makefile,
          # field `bbg_cred` disisipkan ke struct task_security_struct lewat sed
          # di objsec.h, symlink bbg_tracing.h) - sudah dikonfirmasi hasilnya persis
          # sesuai yang diprediksi dari baca source resmi, BUKAN diasumsikan.
          # Tanpa patch ini, kode di tracing/tracing.c (bbg_cred()) tidak akan
          # kompil karena field itu tidak ada di objsec.h asli - jadi langkah
          # setup.sh ini WAJIB untuk kernel non-GKI seperti rosemary, bukan opsional.
          wget -O- https://github.com/vc-teahouse/Baseband-guard/raw/main/setup.sh | bash

          # LSM baru harus didaftarkan ke daftar default LSM stack di security/Kconfig
          # supaya benar-benar aktif tanpa perlu di-set manual lewat cmdline kernel.
          # Ini command RESMI dari README proyek (Quick Start #2), khusus untuk build
          # otomatis/CI - dikutip kata-per-kata dari docs/README-en.md (peringatan
          # resmi: setup.sh --cleanup akan menghapus semua default LSM Kconfig kalau
          # dijalankan, makanya README-nya sendiri menyarankan sed langsung ini untuk
          # skrip build otomatis, bukan menjalankan --cleanup).
          grep -q 'baseband_guard' security/Kconfig && echo "baseband_guard: sudah terdaftar di default LSM list" || \
            sed -i '/^config LSM$/,/^help$/{ /^[[:space:]]*default/ { /baseband_guard/! s/selinux/selinux,baseband_guard/ } }' security/Kconfig
          # CATATAN: kernel 4.14 rosemary-13 ini TIDAK punya blok `config LSM` sama
          # sekali di security/Kconfig (mekanisme stacking itu baru ada di kernel
          # modern yg support DEFINE_LSM). Baseband-guard's Makefile SENDIRI sudah
          # tahu ini (lihat pesan "seems this Kernel doesn't need to check config"
          # di compile log nanti) dan bbg_init() mendaftar sendiri lewat
          # security_initcall() tanpa perlu string CONFIG_LSM apa pun. Jadi pesan
          # di bawah ini SELALU muncul untuk kernel jenis ini - itu bukan tanda
          # ada yang salah, cuma konfirmasi kalau langkah sed di atas memang
          # tidak berlaku (no-op) untuk kernel non-GKI seperti ini.
          grep -n 'baseband_guard' security/Kconfig || echo "baseband_guard: tidak ada blok config LSM di kernel ini - WAJAR untuk non-GKI, hook tetap aktif otomatis lewat security_initcall()"

      - name: Pra-compile - perbaikan build source MTK (verified reusable)
        working-directory: kernel
        run: |
          # 1) buang -Werror standalone dari Makefile/Kbuild (per-folder MTK), jaga -Werror=/-Werror- yg sah
          grep -rlZ --include=Makefile --include=Kbuild --include='*.mk' -e '-Werror' . 2>/dev/null \
            | xargs -0 -r sed -i -E -e 's/-Werror=[A-Za-z0-9_-]+//g' -e 's/(-Werror)([[:space:]]|$)/\2/g' || true
          # 2) buang atribut no_sanitize("...") yg tak dikenal GCC 4.9 (harmless)
          grep -rlZ 'no_sanitize(' . 2>/dev/null \
            | xargs -0 -r sed -i -E 's/__attribute__\(\(no_sanitize\("[^"]*"\)\)\)//g' || true
          # 3) buang 'inline' dari fungsi ber-EXPORT yg body-nya out-of-line
          sed -i 's/inline void ksm_flock(/void ksm_flock(/' include/linux/keyslot-manager.h block/keyslot-manager.c || true
          sed -i 's/^inline void blk_crypto_flock(/void blk_crypto_flock(/' include/linux/blk-crypto.h block/blk-crypto.c || true
          sed -i 's/inline int typec_pd_start_entry(/int typec_pd_start_entry(/' drivers/misc/mediatek/typec/tcpc/inc/tcpci_typec.h drivers/misc/mediatek/typec/tcpc/tcpci_typec.c || true
          # 4) touch Focaltech: stub firmware yg hilang di SEMUA folder driver focaltech
          find drivers/input/touchscreen -name focaltech_config.h -printf '%h\n' 2>/dev/null | while read d; do
            mkdir -p "$d/include/firmware"
            printf '0x00\n' > "$d/include/firmware/fw_sample.i"
            echo "stub firmware -> $d/include/firmware/fw_sample.i"
          done
          # 5) bq2597x: izinkan FP di file ini saja kalau ada (sudah dibungkus kernel_neon_begin/end)
          grep -rl 'bq2597x_charger.o' --include=Makefile . 2>/dev/null | while read mk; do
            echo 'CFLAGS_REMOVE_bq2597x_charger.o += -mgeneral-regs-only' >> "$mk"
            echo "CFLAGS_REMOVE ditambah ke: $mk"
          done
          echo '== cek sisa =='
          echo -n 'no_sanitize( tersisa: '; grep -rl 'no_sanitize(' --include='*.h' --include='*.c' . 2>/dev/null | wc -l

      - name: Aktifkan KSU di defconfig
        working-directory: kernel
        run: |
          DEF=arch/arm64/configs/rosemary_defconfig
          {
            echo ""
            echo "# Penamaan versi kernel - samakan dengan string vendor stock (fsck.f2fs: 4.14.186-perf)."
            echo "# Angka SUBLEVEL sudah diubah ke 186 di Makefile (step lebih awal);"
            echo "# di sini cuma tinggal set suffix '-perf' supaya uname -r jadi '4.14.186-perf' persis."
            echo 'CONFIG_LOCALVERSION="-perf"'
            echo "CONFIG_LOCALVERSION_AUTO=n"
            echo ""
            echo "# ReSukiSU (branch main resmi) - kernel 4.14 non-GKI wajib Manual Hook"
            echo "CONFIG_KSU=y"
            echo "CONFIG_KSU_TRACEPOINT_HOOK=n"
            echo "CONFIG_KSU_MANUAL_HOOK=y"
            echo "CONFIG_KSU_SUSFS=n"
            echo "CONFIG_KALLSYMS=y"
            echo "CONFIG_KALLSYMS_ALL=y"
            echo "# default=y di Kconfig resmi (valid untuk kernel <6.8); ditulis eksplisit"
            echo "# di sini supaya tidak bergantung ke default oldconfig merge - hook"
            echo "# setuid/init-rc/input jadi otomatis lewat LSM, TIDAK perlu patch manual"
            echo "# ke kernel/sys.c, fs/read_write.c, atau drivers/input/input.c."
            echo "CONFIG_KSU_MANUAL_HOOK_AUTO_SETUID_HOOK=y"
            echo "CONFIG_KSU_MANUAL_HOOK_AUTO_INITRC_HOOK=y"
            echo "CONFIG_KSU_MANUAL_HOOK_AUTO_INPUT_HOOK=y"
            echo ""
            echo "# NoMount"
            echo "CONFIG_NOMOUNT=y"
            echo ""
            echo "# Re:Kernel"
            echo "CONFIG_REKERNEL=y"
            echo ""
            echo "# Baseband-guard"
            echo "CONFIG_BBG=y"
            echo ""
          } >> "$DEF"
          tail -25 "$DEF"

      - name: Ambil toolchain proton-clang (dipakai penulis kernel ini di b.sh)
        run: |
          git clone --depth=1 https://github.com/kdrag0n/proton-clang.git clang-proton

      - name: Compile kernel
        working-directory: kernel
        run: |
          export PATH="$GITHUB_WORKSPACE/clang-proton/bin:$PATH"
          export ARCH=arm64
          export SUBARCH=arm64
          export KBUILD_BUILD_USER=build
          export KBUILD_BUILD_HOST=kernel
          # Timestamp build ditetapkan manual (bukan waktu build CI sebenarnya) supaya
          # cocok dengan tanggal pembaruan keamanan yang ditampilkan di HP (2024-07-01).
          # Format persis output 'date' asli Linux: nama hari/bulan singkatan 3 huruf,
          # tanggal di-pad SPASI (bukan nol) kalau <10 - 1 Juli 2024 jatuh hari SENIN,
          # bukan Sabtu, supaya hari & tanggal tetap konsisten satu sama lain.
          export KBUILD_BUILD_TIMESTAMP="Mon Jul  1 17:32:55 CST 2024"
          make O=out rosemary_defconfig

          echo ""
          echo "==================== RESOLVED ReSukiSU CONFIG ===================="
          grep -E '^(CONFIG_KSU|CONFIG_KALLSYMS)' out/.config || true

          grep -q '^CONFIG_KSU=y$' out/.config || {
            echo "ERROR: CONFIG_KSU tidak resolve ke y"
            exit 1
          }
          grep -q '^CONFIG_KSU_MANUAL_HOOK=y$' out/.config || {
            echo "ERROR: CONFIG_KSU_MANUAL_HOOK tidak resolve ke y"
            exit 1
          }
          grep -q '^CONFIG_KALLSYMS=y$' out/.config || {
            echo "ERROR: CONFIG_KALLSYMS tidak resolve ke y"
            exit 1
          }
          grep -q '^CONFIG_KALLSYMS_ALL=y$' out/.config || {
            echo "ERROR: CONFIG_KALLSYMS_ALL tidak resolve ke y"
            exit 1
          }

          if grep -q '^CONFIG_KSU_TRACEPOINT_HOOK=y$' out/.config; then
            echo "ERROR: CONFIG_KSU_TRACEPOINT_HOOK=y aktif; kernel 4.14 harus memakai Manual Hook"
            exit 1
          fi

          if grep -q '^CONFIG_KSU_SUSFS=y$' out/.config; then
            echo "ERROR: CONFIG_KSU_SUSFS=y aktif; build ini ditujukan untuk Manual Hook tanpa SUSFS"
            exit 1
          fi

          echo ""
          echo "==================== RESOLVED NoMount CONFIG ===================="
          grep -E '^CONFIG_NOMOUNT' out/.config || true
          grep -q '^CONFIG_NOMOUNT=y$' out/.config || {
            echo "ERROR: CONFIG_NOMOUNT tidak resolve ke y"
            exit 1
          }

          echo ""
          echo "==================== RESOLVED Baseband-guard CONFIG ===================="
          grep -E '^CONFIG_BBG' out/.config || true
          grep -q '^CONFIG_BBG=y$' out/.config || {
            echo "ERROR: CONFIG_BBG tidak resolve ke y (cek CONFIG_SECURITY=y - CONFIG_BBG depends on itu)"
            exit 1
          }

          echo "ReSukiSU Manual Hook + auto-hook: CONFIG OK"

          set -o pipefail
          if ! make -k O=out -j"$(nproc)" \
                ARCH=arm64 \
                CC=clang \
                AR=llvm-ar \
                NM=llvm-nm \
                OBJCOPY=llvm-objcopy \
                OBJDUMP=llvm-objdump \
                STRIP=llvm-strip \
                CROSS_COMPILE=aarch64-linux-gnu- \
                CROSS_COMPILE_ARM32=arm-linux-gnueabi- \
                CONFIG_SECTION_MISMATCH_WARN_ONLY=y \
                KCFLAGS="-Wno-error" 2>&1 | tee /tmp/build.log; then
            echo ""
            echo "==================== BARIS ERROR (kirim ini) ===================="
            grep -niE "error:|fatal error|undefined reference|No such file|multiple definition|recipe for target|Error [0-9]" /tmp/build.log | tail -120 || true
            echo "==================== KONTEKS TIAP KEGAGALAN (kirim ini juga) ===================="
            for L in $(grep -n 'Error 1' /tmp/build.log | head -3 | cut -d: -f1); do
              S=$((L-40)); [ $S -lt 1 ] && S=1
              echo "----- konteks baris $S s/d $L -----"
              sed -n "${S},${L}p" /tmp/build.log
            done
            echo "==================== 50 BARIS TERAKHIR ===================="
            tail -50 /tmp/build.log
            echo ""
            echo ">>> BUILD GAGAL. Salin blok BARIS ERROR di atas dan kirim balik."
            exit 1
          fi
          ls -la out/arch/arm64/boot/
          echo ""
          echo "==================== VERIFIKASI MANUAL HOOK (resmi) ===================="
          echo "Pesan ini berasal dari kernel/tools/manual_hook_check.mk milik ReSukiSU"
          echo "branch main (di-include otomatis oleh kernel/Kbuild saat CONFIG_KSU_MANUAL_HOOK=y)."
          echo "Kalau build lolos sampai sini, SEMUA hook wajib sudah ditemukan - mk ini"
          echo "hard-fail (\$(error ...)) begitu ada simbol yang hilang, bukan skip diam-diam."
          echo ""
          echo "== Pesan hook dari Kbuild =="
          grep -iE "resukisu.*manual_hook|using manual hook|using a manual|using lsm hooks" /tmp/build.log || true

      - name: Compile nm + rakit modul flashable NoMount (commit sama dgn kernel)
        run: |
          # PENTING: modul flashable apa pun yang didownload dari sumber lain
          # TIDAK ADA jaminan cocok dengan kernel yang barusan dikompilasi
          # (risiko drift commit / protokol kernel<->userspace). Fix: compile
          # nm DAN rakit module.zip di sini memakai clone kernel/NoMount YANG
          # SAMA PERSIS (commit sama) dengan step "Integrasikan NoMount" di atas
          # - TIDAK clone ulang - supaya kernel driver dan modul userspace
          # dijamin satu commit (branch: dev).
          NM_SRC="kernel/NoMount/userspace/src"
          MOD_SRC="kernel/NoMount/module"
          test -d "$NM_SRC" || { echo "ERROR: $NM_SRC tidak ada - step Integrasikan NoMount gagal?"; exit 1; }
          test -d "$MOD_SRC" || { echo "ERROR: $MOD_SRC tidak ada - struktur repo NoMount berubah?"; exit 1; }

          NOMOUNT_COMMIT=$(git -C kernel/NoMount rev-parse --short HEAD)
          echo "NoMount commit dipakai (HARUS SAMA dgn step Integrasikan NoMount di atas): $NOMOUNT_COMMIT"

          # 1) Compile nm (freestanding, syscall mentah - tanpa libc, sesuai desain
          #    nm.h yang pakai inline asm syscall langsung, bukan wrapper glibc).
          mkdir -p nm-src
          cp "$NM_SRC/nm.c" "$NM_SRC/nm.h" nm-src/
          ( cd nm-src && \
            aarch64-linux-gnu-gcc -nostdlib -static -Os -fno-builtin -Wl,-e,_start -o nm nm.c && \
            aarch64-linux-gnu-strip nm )
          file nm-src/nm
          test -s nm-src/nm || { echo "ERROR: nm gagal dikompilasi"; exit 1; }

          # 2) Compile ko-loader-arm64 (BUKAN freestanding - pakai stdio/stdlib biasa,
          #    jadi -static saja, TANPA -nostdlib). Header .ko di-generate via xxd -i
          #    dari assets/*.ko bawaan repo ko-loader, sesuai Makefile resminya -
          #    cuma CC diganti ke gcc-aarch64-linux-gnu karena runner ini tidak
          #    punya zig (Makefile resmi pakai `zig cc -target aarch64-linux`, hasil
          #    akhirnya setara: ELF aarch64 statis, sudah diuji sukses di sandbox).
          rm -rf ko-loader-src
          git clone --depth=1 https://github.com/maxsteeel/ko-loader.git ko-loader-src
          ( cd ko-loader-src && \
            mkdir -p src/include && \
            cd assets && \
            for f in *.ko; do \
              xxd -i "$f" > "../src/include/${f%.ko}.h"; \
            done && \
            cd .. && \
            aarch64-linux-gnu-gcc -s -Oz -static -Wl,--gc-sections,-z,norelro \
              -fno-unwind-tables -fno-ident -fmerge-all-constants \
              src/ko-loader.c -o ko-loader-arm64 )
          # PENTING: xxd -i harus dijalankan SETELAH `cd assets` (bukan xxd -i
          # assets/nama.ko dari luar) - kalau tidak, xxd akan menamai variabel C
          # dengan prefix "assets_..." (dari path lengkap), padahal ko-loader.c
          # mengharapkan nama TANPA prefix folder (persis pola resmi di Makefile
          # repo ini: "cd $(ASSETS_DIR) && xxd -i $(notdir $<)"). Sudah diuji
          # sungguhan di sandbox: variabel yang dihasilkan persis cocok
          # (android12_5_10_lkmloader_ko, dst) dan ko-loader.c compile bersih.
          file ko-loader-src/ko-loader-arm64
          test -s ko-loader-src/ko-loader-arm64 || { echo "ERROR: ko-loader-arm64 gagal dikompilasi"; exit 1; }

          # 3) Rakit folder module/ jadi module.zip flashable. Format KernelSU/
          #    Magisk modern tidak butuh META-INF/update-binary (ksud yang
          #    menangani ekstraksi) - cukup module.prop + customize.sh + file
          #    lain di root zip, sesuai cara resmi build.yml sendiri meng-upload
          #    folder module/ apa adanya.
          rm -rf module-build
          cp -r "$MOD_SRC" module-build
          mkdir -p module-build/bin
          cp nm-src/nm module-build/bin/nm-arm64
          cp ko-loader-src/ko-loader-arm64 module-build/bin/ko-loader-arm64
          chmod 0755 module-build/bin/nm-arm64 module-build/bin/ko-loader-arm64

          # versionCode diisi commit NoMount (bukan angka run CI) supaya modul ini
          # bisa dilacak persis dibangun dari commit mana - beda dari cara resmi
          # (yg pakai github.com/run_number) karena workflow ini bukan CI resmi
          # NoMount, tapi tetap harus traceable.
          sed -i "s/^versionCode=.*/versionCode=${NOMOUNT_COMMIT}/" module-build/module.prop
          echo "== module.prop =="; cat module-build/module.prop

          ZIP_MOD="nomount-dev-module-${NOMOUNT_COMMIT}.zip"
          ( cd module-build && zip -r9 "../$ZIP_MOD" . -x '.git*' )
          echo "MOD_ZIP=$ZIP_MOD" >> "$GITHUB_ENV"
          echo ""
          echo "== isi module.zip =="
          unzip -l "$ZIP_MOD"

          echo ""
          echo ">>> FLASH MODUL INI (bukan modul dari sumber lain) lewat KernelSU Manager"
          echo ">>> supaya protokol kernel<->userspace cocok persis dgn kernel yang barusan dikompilasi."

      - name: Tentukan file kernel hasil
        id: kimg
        run: |
          FOUND=""
          for f in Image.gz-dtb Image.gz Image; do
            p="kernel/out/arch/arm64/boot/$f"
            if [ -f "$p" ]; then
              echo "path=$p" >> "$GITHUB_OUTPUT"
              echo "name=$f" >> "$GITHUB_OUTPUT"
              FOUND=1
              break
            fi
          done
          [ -n "$FOUND" ] || { echo "Kernel image tidak ditemukan"; exit 1; }
          DTBO="kernel/out/arch/arm64/boot/dtbo.img"
          if [ -f "$DTBO" ]; then
            echo "dtbo=$DTBO" >> "$GITHUB_OUTPUT"
            echo "dtbo.img ditemukan: $DTBO"
          fi

      - name: Rakit paket AnyKernel3 (template asli fjrXTR)
        run: |
          git clone --depth=1 https://github.com/fjrXTR/AnyKernel3.git AnyKernel3
          rm -rf AnyKernel3/.git

          echo "" | base64 -d > AnyKernel3/anykernel.sh
          echo "== anykernel.sh =="; cat AnyKernel3/anykernel.sh

          cp "${{ steps.kimg.outputs.path }}" "AnyKernel3/${{ steps.kimg.outputs.name }}"

          # Nama file dibuat unik per-run supaya tidak bentrok antar build.
          ZIP="rosemary-ksu-nomount-rekernel-bbg-$(date +%Y%m%d-%H%M%S).zip"

          ( cd AnyKernel3 && zip -r9 "../$ZIP" . -x '.git*' )
          echo "ZIP=$ZIP" >> "$GITHUB_ENV"
          unzip -l "$ZIP" | head -20

      - name: Upload paket + kernel mentah + dtbo + nm CLI + modul NoMount
        uses: actions/upload-artifact@v4
        with:
          if-no-files-found: warn
          path: |
            ${{ env.ZIP }}
            ${{ env.MOD_ZIP }}
            ${{ steps.kimg.outputs.path }}
            ${{ steps.kimg.outputs.dtbo }}
            nm-src/nm
            kernel/out/.config
